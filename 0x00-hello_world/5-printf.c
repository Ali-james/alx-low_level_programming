#include <stdio.h>

/**
 * main -Entry there is no much comment to make
 * Return: Always 0
 */
int main(void)
 {
	 printf("with proper grammar, but the outcome is a piece of art,\n");
 Return (0);
 }
